# Lao font - Saysettha OT

Place the Lao font files here:

- `Saysettha-Regular.ttf` for weight 400
- `Saysettha-Bold.ttf` for weight 700

The app already references these files from `src/app/globals.css`.
If the files are missing, the browser falls back to `Noto Sans Lao` or the system font.
